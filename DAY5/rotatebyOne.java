// List<Integer> list = new ArrayList<>();
//         for (int num : arr) list.add(num); // Convert array to list

//         Collections.rotate(list, 1); // Rotate right by 1

//         // Copy back to array
//         for (int i = 0; i < arr.length; i++) {
//             arr[i] = list.get(i);
//         }